import 'package:flutter/material.dart';
import '../main.dart';

class AlarmPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Alarm')),
      body: Center(
        child: Text('Alarm sayfası'),
      ),
    );
  }
}
